spring:
  application:
    name: cloud-config
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: cloud-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8910/eureka/

server:
  port: 8909